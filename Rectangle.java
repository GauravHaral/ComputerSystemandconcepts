// Rectangle using Object
public class Rectangle {
  int length;
  int width;
  public int calcArea() {
     return (length * width);
  }
  public int calcPerimeter() {
     return ( 2 * (length + width) );
  }
}
